import { useContext } from 'react';
import { QuizContext } from '../../context/quizContext';

import './Option.css';

const Option = () => {
  return (
    <div>Option</div>
  );
};

export default Option;
